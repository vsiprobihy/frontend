import Button from "@/app/components/buttons/Button";

const text = `Скинути всі фільтри`;

//   TODO complete logic

const CalendarFilterResetButton: React.FC = () => {
  const onClick = () => {
    //   TODO
  };

  return <Button onClick={onClick}>{text}</Button>;
};

export default CalendarFilterResetButton;
