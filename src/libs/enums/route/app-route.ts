// TODO: Add other routes if necessary

export enum AppRoute {
  ANY = "*",
  ROOT = "/",
  CALENDAR = "/calendar",
  ABOUT = "/about",
  NOT_FOUND = "/404",
  EVENT = "/event/events",
  PROFILE = "/profile",
}
